const collection = document.forms;
var currencyRegex = /[.,\s]/g;

form.addEventListener("submit", (e) => {
    let flashes =  []
    for (const item in collection) {
        // Get and validate each input field
        // If field is valid, append to JSON key:value[]
}})

console.log("ELLOOO I AM HIAR")